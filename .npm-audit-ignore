# npm audit ignore list — one GHSA id per line; `#` starts a comment.
# Read by app1/family-fund-app/bin/security-scan.sh (run_npm_audit) and
# bin/npm-audit-gate.cjs. Keep this narrow: only accept advisories that
# genuinely don't affect what we ship, and note the remediation plan.
#
# Currently empty. The former Vite dev-server advisory cluster
# (GHSA-67mh-4wv8-2f99, GHSA-93m4-6634-74q7, GHSA-4w7w-66w2-5vf9,
# GHSA-g4jq-h2w9-997c, GHSA-jqfw-vq24-v9c3 — all dev-server-only on
# vite@4 / bundled esbuild@0.18) was resolved by upgrading vite 4 -> 6
# (esbuild >= 0.25), which also cleared the laravel-vite-plugin peer
# mismatch. See #34. `npm audit` now reports 0 vulnerabilities.
