# Dusk UI failure log (2026-05-21)

> **Status update (2026-05-21, after full fix series):**
> **All UI bugs ✅ closed. Final Dusk suite: 27 passed / 0 failed**
> (was 5 / 22). Every failure was test-code drift against UI changes
> that shipped without the Dusk suite being updated — no application
> bugs were found in this pass. Summary:
> - **#ui-1** — missing required `nickname` field on the create form
>   (HTML5 validation blocked submit → 21 timeouts).
> - **#ui-2** — Dusk `->type()` on an HTML5 date input garbled the year
>   to `0421`; switched to JS `.value=...` + `dispatchEvent('change')`.
> - **#ui-3** — repay moved into a Bootstrap modal, readjust moved to a
>   `/actions` sub-page; Page Object refactored.
> - **#ui-4** — account tile shows cumulative `−{n} sh borrowed`, not a
>   per-line `120.00 borrowed`; relaxed assertion.
> - **#ui-5** — over-borrow JS gate, flash-text drift, and the
>   cancel/readjust-moved-to-`/actions` issues.
> - **#ui-6** — `Credit Line #N`→`Loan Share #N` rename; `FUND_ID=1`
>   doesn't exist (account 7 is fund 2); bucket stripe is on the fund
>   show page, not `/overview`.
> - **#ui-7** — 30-day backdate left the first row inside grace (bumped
>   to 90d); pill text is lowercase `late`.
> - **#ui-bb1** — `testpool.sh tour` now defaults to the full
>   `tests/Browser/` suite; `SKILL.md` updated.

Companion to [`QA_BUGS_2026-05-21.md`](QA_BUGS_2026-05-21.md) (PHPUnit
findings) and [`QA_BUGS_2026-05-20.md`](QA_BUGS_2026-05-20.md) (round-by-round
investigation log). This file is **Browser-suite only** — what
`php artisan dusk` reports when the whole `tests/Browser/` tree runs against
a freshly-claimed `testpool` slot.

Source: full Dusk run on a leased **testpool `test1`** slot (isolated DB
`familyfund_test1`, restored from the committed baseline) on branch
`worktree-bridge-cse_01BvT6ZgRnhw2V6MJDABBT6P` (which equals `main` at
`11562c87` after this thread's merge). Run via:

```bash
~/.familyfund-pool/testpool.sh tour test1 -- tests/Browser/
```

Wall time ≈ 4m. Selenium sidecar: `seleniarm/standalone-chromium:latest`
(Chromium 124.0.6367.78). Screenshots: `tests/Browser/screenshots/tour/`.

- **Totals:** 27 tests, 14 assertions; **22 failed, 5 passed**.
- The 5 passing are all non-admin ACL 403 checks (HTTP-level only — no JS
  interaction). Every test that actually drives a form via JS fails.
- **Pre-flight required:** the testpool baseline still doesn't include the
  spatie + QA-user seeders, so before any Dusk run can reach dev-login you
  must `migrate --force` + `db:seed --class=RolesAndPermissionsSeeder` inside
  the slot container. `QaTestUsersSeeder` then partially seeds — see
  [#14 in QA_BUGS_2026-05-21](QA_BUGS_2026-05-21.md#p2--14-qatestusersseeder-writes-non-existent-accountstype-column).
  This means **today the Browser suite cannot be run cleanly without manual
  prep** — that, plus the failure pattern below, points at a shared root
  cause rather than 22 independent bugs.

> Severity matches `QA_BUGS_2026-05-20.md`:
> **S1** = data integrity · **S2** = broken feature · **S3** = bad UX ·
> **S4** = cosmetic.
>
> Priority: **P0** = fix before next merge · **P1** = fix this iteration ·
> **P2** = fix soon.

## Working hypothesis (one root cause, ~21 of 22 failures)

Every test that fills a form and presses submit hits the same shape:

1. The form **renders correctly** — screenshot `04_create_form_filled.png`
   from `CreditLineUITourTest` shows all 7 inputs populated, the
   "Available to borrow as of 2026-05-21: 4111.0336 shares" hint visible,
   and the "Open" button enabled.
2. The test calls `->press('Open')` (or equivalent).
3. The page **never redirects** to `/credit-lines/{id}`; Dusk waits 10 s,
   times out, and the test fails with `TimeoutException`.
4. No follow-up screenshot is captured (the assertion fired before the next
   `->screenshot()` call), so the post-press state is invisible — but the
   one place we *did* see post-press state
   (`S9LateNotificationSmokeTest`) shows the submit button is
   `disabled=""` and another element is intercepting the click.

The shared root cause is therefore most likely **the submit handler doesn't
fire / the form action is interrupted by client-side validation**, not 21
distinct backend bugs. Likely candidates:

- A recent change to the credit-line create / repay forms introduced a
  client-side disabled-until-valid gate (matches `d8afb7fb fix(ui):
  credit-line create form over-borrow warning + empty-state` and
  `d1c87884 feat(credit-line): single Make payment button + Fill remainders
  allocate UX` in `git log`), and Dusk's `->type()` / `->select()` calls
  don't trigger the `input`/`change` events the gate listens for.
- Stale Vite assets in the slot image — the `testpool.sh claim` flow doesn't
  rebuild `public/build/*` against the worktree's `resources/` tree, so the
  compiled JS in the container can be from an older asset bundle (the warm
  cache from another branch).
- A `wire:` / Livewire payload that Dusk isn't waiting on before pressing
  submit.

This is **one investigation** to start, not 22.

## Priority order

| Pri | Sev | Bug ID                                                      | Status | One-liner |
| --- | --- | ----------------------------------------------------------- | ------ | --------- |
| P0  | S2  | [#ui-1](#p0--ui-1-form-submit-via-dusk-never-redirects)     | ✅ closed (test-code; 9 files patched) | The shared root cause: tests didn't fill the now-required `nickname` field on the create form. HTML5 native validation silently blocked submit. Adding `->type('input[name="nickname"]', ...)` to each test's form-fill sequence drops the 21 timeouts. |
| P1  | S2  | [#ui-2](#p1--ui-2-submit-button-disabled-overlap-on-s9)     | ✅ closed (test-code) | `S9LateNotificationSmokeTest` was using `->type('input[name="origination_date"]', '2026-04-21')` which Chrome's native date picker parses character-by-character into separate month/day/year segments → produced year `0421`. The form's JS fetched available-to-borrow for that bogus past date, got `0`, the over-borrow gate disabled submit, and the click was intercepted. Fixed by setting the date via `$browser->script()` (`.value = ...; dispatchEvent('change')`). Test now drives past the form. |
| P1  | S2  | [#ui-7](#p1--ui-7-s9-schedule-table-missing-late-label)      | ✅ closed (test-code) | Two test-code issues: (1) the 30-day backdate produced a schedule whose first row is due today — inside the 3-day grace, so no row gets flagged late. Bumped backdate to 90 days. (2) The badge text is lowercase `late` (`_payment_status_badge.blade.php:11`), not capitalized `Late`. Relaxed both. |
| P1  | S2  | [#ui-3](#p1--ui-3-creditlineborrowingflowtest-downstream-failures) | ✅ closed (test-code, Page Object refactor) | The repay form moved into a Bootstrap modal triggered by "Make payment", and the readjust form moved to a separate `/credit-lines/{id}/actions` page. `CreditLineShowPage::repay()` now opens the modal first; `::readjust()` navigates to actions. The 6-row count assertion was matching too many tables — narrowed to `tbody tr[data-due]`. All 6 `CreditLineBorrowingFlowTest` cases pass. |
| P1  | S2  | [#ui-4](#p1--ui-4-creditlineuitourtest-asserts-12000-borrowed) | ✅ closed (test-code) | The blade renders `−{n} sh borrowed` (cumulative across the account's CLs), not `{n}.00 borrowed`. On the shared test baseline account 7 already has CLs, so the displayed number is cumulative, not just the new 120. Relaxed to `assertSee('sh borrowed')`. |
| P1  | S2  | [#ui-5](#p1--ui-5-creditlinenegativeuitest-residuals)       | ✅ closed (test-code) | 4 distinct test-code issues: `over_borrow` JS gate disabled submit → bypass with `form.submit()`; `over_borrow` expected `Cannot borrow` text but form-request short-circuits with a different message → assert `OWN minus outstanding on active lines`; `cancel_active_line` looked for "Cancel line" on the show page but it moved to `/actions`; `readjust_no_change` set `value` via JS on a form that no longer exists on show page → visit `/actions` first. All 12 Negative tests now pass. |
| P1  | S2  | [#ui-6](#p1--ui-6-scenario-suites-3-fail)                   | ✅ closed (test-code) | `S1` passed once #ui-1+#ui-3 landed. `S5` asserted the old `Credit Line #N` heading → renamed `Loan Share #N`. `S7` used `FUND_ID=1` (doesn't exist; account 7 is in fund 2) and looked for the bucket stripe on `/overview` when it lives on the fund **show** page. All 4 Scenario suites pass. |
| P2  | S4  | [#ui-bb1](#p2--ui-bb1-tour-skill-hardcodes-credit-line-filter) | ✅ closed | `testpool.sh tour` now defaults to `tests/Browser/` (the full suite). The `--filter=...` override remains for single-class runs. `SKILL.md` updated to document the new default + override pattern. |

---

## P0 / #ui-1 — Form submit via Dusk never redirects

**✅ Closed in test code.** Root cause was a recent backend addition the
tests hadn't been updated for: `e62d7465 wip(credit-line): nickname +
effective-date migrations` added `nickname` as both an HTML5 `required`
input on `account_credit_lines/create.blade.php:50` AND a
`required|string|max:255` rule in `CreateAccountCreditLineRequest.php:22`.
None of the Dusk tests that drove this form ever typed into the nickname
field — Chrome's native form validation silently aborted the submit when
`->press('Open')` fired, so the URL never changed and Dusk waited the full
10 s.

The screenshot evidence was right there: `04_create_form_filled.png` shows
the nickname field with only its placeholder text (`e.g. Kitchen renovation
loan`) while every other field is populated. The "Open" button being
visually enabled was a red herring — HTML5 doesn't disable the submit
button, it just refuses to POST.

**Fix (commit landing on `main` with this update):** add
`->type('input[name="nickname"]', '<label>')` to the form-fill sequence in
every offending test. 9 files touched:

- `tests/Browser/CreditLineAllocateUITest.php`
- `tests/Browser/CreditLineBorrowingFlowTest.php`
- `tests/Browser/CreditLineNegativeUITest.php` (4 sites)
- `tests/Browser/CreditLineSimulatorUITest.php` (2 sites)
- `tests/Browser/CreditLineUITourTest.php`
- `tests/Browser/Scenarios/S1MultiLineSmokeTest.php`
- `tests/Browser/Scenarios/S5GoalWithBorrowingSmokeTest.php`
- `tests/Browser/Scenarios/S7FundSummarySmokeTest.php`
- `tests/Browser/Scenarios/S9LateNotificationSmokeTest.php`

**Result of fix:** Dusk now reports **14 passed / 13 failed** (was 5 / 22).
9 net additional tests pass; the 13 remaining failures have **different**
exception types and are downstream issues that the original timeout was
masking. They're documented separately as #ui-3..#ui-6 below.

### Original investigation (kept for provenance)

- **Affected tests** (21 of 22, all `TimeoutException` at 10 s):
  - `CreditLineAllocateUITest`
    - `fill_remainders_balances_form_and_enables_submit`
    - `server_rejects_under_allocation_with_flash_error`
  - `CreditLineBorrowingFlowTest`
    - `admin_can_open_credit_line_through_ui`
    - `repay_advances_schedule_and_reduces_outstanding`
    - `readjust_creates_audit_row_and_new_schedule`
    - `reverse_reopens_schedule_row`
    - `loans_summary_card_reflects_account_state`
    - `trajectory_chart_renders`
  - `CreditLineNegativeUITest`
    - `open_credit_line_with_negative_principal_shows_validation_error`
    - `open_credit_line_with_zero_term_shows_validation_error`
    - `over_borrow_shows_error`
    - `cancel_active_line_with_outstanding_blocks`
    - `account_show_blocked_when_active_credit_line_exists`
    - `readjust_no_change_shows_error`
    - `reverse_already_reversed_transaction_fails`
  - `CreditLineSimulatorUITest`
    - `simulator_renders_form_and_chart`
    - `simulator_time_mode_renders`
  - `CreditLineUITourTest`
    - `ui_tour_of_credit_line_flow` (fails at line 56 — the
      `waitUsing(10, 200, …/credit-lines/\d+$/)` after `->press('Open')`)
  - `Tests\Browser\Scenarios\S1MultiLineSmokeTest::two_lines_open_then_first_paid_off_via_ui`
  - `Tests\Browser\Scenarios\S5GoalWithBorrowingSmokeTest::goal_progress_card_renders_while_a_borrowing_is_active`
  - `Tests\Browser\Scenarios\S7FundSummarySmokeTest::fund_summary_renders_loaned_bucket_under_active_borrowing`

- **Smoking gun:** `tests/Browser/screenshots/tour/04_create_form_filled.png`
  from the `CreditLineUITourTest` run on this branch. Form is fully
  populated, "Open" button visible and styled enabled. The very next
  expected screenshot `05_show_page_just_opened.png` was never captured —
  the wait timed out first.

- **Reproducer:**
  ```bash
  cd <worktree>/app1
  ~/.familyfund-pool/testpool.sh claim ui-debug
  SLOT=$(~/.familyfund-pool/testpool.sh list | awk '/ui-debug/ {print $1}')
  docker exec "familyfund-${SLOT}" php artisan migrate --force
  docker exec "familyfund-${SLOT}" php artisan db:seed --class=RolesAndPermissionsSeeder --force
  ~/.familyfund-pool/testpool.sh tour "${SLOT}" -- tests/Browser/CreditLineUITourTest.php
  # → TimeoutException at CreditLineUITourTest.php:56
  ls family-fund-app/tests/Browser/screenshots/tour/   # see 01..04 saved, 05 absent
  ```

- **Suggested first probe:** add a `->screenshot('debug_post_press')` and
  `->dump()` call immediately after the `->press('Open')` in
  `CreditLineUITourTest::test_ui_tour_of_credit_line_flow`. Either:
  - the page is the same `/accounts/7/credit-lines/create` URL with a
    server-side validation error rendered (backend rejected the submit), or
  - the URL changed to `/accounts/7/credit-lines` (form posted but the
    controller returned to the index without a redirect-to-show), or
  - the URL didn't change at all (client-side JS prevented submit).

  The third case is most likely given recent client-side changes to the
  create form; the first two would expose a separate backend bug.

- **Likely fix surface:** if it's case (3), the Dusk `->type()` calls need a
  follow-up `$browser->keys($selector, [' ', '{backspace}'])` or
  `->fireEvent('input')` to nudge the JS validity gate. If it's case (1)/(2),
  the form-request rules likely need adjusting, OR the controller's redirect
  target is wrong after recent allocation-ledger changes.

## P1 / #ui-3 — CreditLineBorrowingFlowTest downstream failures

**✅ Closed in test code via Page Object refactor.** Three things had
changed in the UI since the tests were written; the Page Object hadn't
caught up:

1. **Repay moved into a modal.** The show page's "Record repayment"
   inline form was replaced by a Bootstrap modal (`#makePaymentModal`)
   triggered by a green "Make payment" button (`show.blade.php:153-156`,
   modal at line 241). Dusk's `->type('form[action$="/repay"] input[name="shares"]', ...)`
   failed with `ElementNotInteractableException` because the modal
   contents are hidden until the trigger button is clicked. Refactored
   `CreditLineShowPage::repay()` to click the trigger, wait for
   `#makePaymentModal.show`, type into `#makePaymentShares`, submit, and
   wait for the modal to dismiss.
2. **Readjust moved to a sub-page.** The readjust form lives at
   `/credit-lines/{id}/actions` now (`actions.blade.php:48-72`,
   route `credit_lines.actions` at `web.php:274-276`). Updated
   `::readjust()` to navigate there before filling the form.
3. **Button label changed.** "Record repayment" → "Record payment" on
   the modal submit. Switched from `->press('Record repayment')` to
   `->click('#makePaymentModal button[type="submit"]')`, which is
   future-proof against further label tweaks.

For `admin_can_open_credit_line_through_ui`, the schedule-row assertion
was matching too many `<tr>` elements because the show page has
multiple `.card .table` blocks (schedule, payment allocations,
adjustment history). Narrowed the selector to `tbody tr[data-due]` —
schedule rows carry `data-status` + `data-due` attributes from
`show.blade.php:195` and no other table does.

All 6 `CreditLineBorrowingFlowTest` cases now pass (30 assertions).

## P1 / #ui-4 — CreditLineUITourTest asserts "120.00 borrowed"

**✅ Closed in test code.** The account show page renders the borrowed
sub-line as `−{n} sh borrowed` (`show_ext.blade.php:72,79`), and the
displayed number is **cumulative** across all the account's active CLs,
not just the freshly-opened one. On the committed test baseline,
account 7 already has several CLs (~1,655 sh outstanding), so the
displayed value after opening +120 is ~1,775 sh, never `120.00`. Relaxed
the assertion to `assertSee('sh borrowed')` — proves the sub-line is
present without baking the baseline state into the test.

The tour test now passes assertion 8/N. The next failure is a downstream
`ElementNotInteractableException` on the repay form
(`form[action$="/repay"] input[name="shares"]`) at line 95 — overlaps
with #ui-3's territory and will be addressed there.

## P1 / #ui-5 — CreditLineNegativeUITest residuals

**✅ Closed in test code.** Four distinct issues, all test-code:

1. **`over_borrow_shows_error` — JS gate intercepted submit click.**
   The form's `syncOverBorrow` correctly disables the submit button when
   `principal > available` (i.e. the gate is working). The test wanted to
   exercise the **server-side** OverBorrowException / form-request
   validation path, so the click was intentional. Replaced
   `->press('Open')` with a script call that strips the `disabled`
   attribute and invokes `form.submit()` directly — the gate stays
   intact for real users but the test can probe the server path.

2. **`over_borrow_shows_error` — assertion text drift.**
   The visible flash on the form was
   `Only N shares available to borrow as of D (OWN minus outstanding
   on active lines)`, not `Cannot borrow …`. Two paths produce two
   messages: `CreateAccountCreditLineRequest.php:64` (form-request
   validator, fires first) and `OverBorrowException.php:21`
   (service layer, never reached because of the form-request short
   circuit). Relaxed the assertion to
   `assertSee('OWN minus outstanding on active lines')` — that phrase
   appears in both variants.

3. **`cancel_active_line_with_outstanding_blocks` — "Cancel line"
   moved.** The button now lives at `/credit-lines/{id}/actions`
   (`actions.blade.php:81`), not on the show page. Test now navigates
   there before pressing.

4. **`readjust_no_change_shows_error` — same root cause as #ui-3 for
   readjust.** The test was using
   `document.querySelector('form[action*="readjust"] input[name="new_term_months"]').value = ...`
   on the show page; that form doesn't exist there any more. Test now
   visits `/credit-lines/{id}/actions` first.

All 12 `CreditLineNegativeUITest` cases pass (22 assertions).

## P1 / #ui-6 — Scenario suites: 3 fail post-form-submit

**✅ Closed in test code.**

- `S1MultiLineSmokeTest` — passed automatically once #ui-1 (nickname) and
  #ui-3 (modal/actions Page Object refactor) landed; it drives the same
  open/repay flows through the shared Page Object.
- `S5GoalWithBorrowingSmokeTest` — asserted `assertSee('Credit Line #N')`
  on the line page; the heading was renamed to `Loan Share #N` (UI label
  rename — routes + tables still use `credit_line`). Switched the
  assertion to `Loan Share #N`.
- `S7FundSummarySmokeTest` — two bugs: (1) `FUND_ID = 1`, but fund 1
  doesn't exist in the committed baseline and account 7 (the one opening
  the loan share) is in **fund 2** — so `/funds/1/overview` redirected to
  `/funds` and the `waitForLocation` timed out. Set `FUND_ID = 2`.
  (2) The "Allocated / Loaned / Unallocated" bucket stripe lives on the
  fund **show** page (`show_ext.blade.php:197-218`), not the overview
  page — changed the visit target from `/funds/2/overview` to `/funds/2`.

The `CreditLineUITourTest` (#ui-4 follow-on) also had its own inline
repay/readjust calls using the old `form[action$="/repay"]` /
`form[action$="/readjust"]` selectors. Switched those to the shared
`CreditLineShowPage::repay()` / `::readjust()` helpers so they use the
modal + actions-page flows.

**Final Dusk suite: 27 passed (93 assertions), 0 failed** — from the
original 5 passed / 22 failed.

## P1 / #ui-2 — Submit button disabled + overlap on S9

**✅ Closed in test code.** Originally read as "two separable problems"
(button disabled AND form overlapping the click) — one root cause, not two:

- The test was using
  `$browser->clear('input[name="origination_date"]')->type('input[name="origination_date"]', '2026-04-21')`
  to backdate the origination by 30 days.
- Chrome's native `<input type="date">` doesn't take a single string into
  its `value` from keyboard input — `->type()` enters characters one at a
  time into the **month / day / year segments**. The intended `2026-04-21`
  was parsed as month `20`, day `26`, year `0421` (visible in the failure
  screenshot:
  `tests/Browser/screenshots/failure-Tests_Browser_Scenarios_S9LateNotificationSmokeTest_test_loan_share_show_renders_late_state_for_backdated_line-0.png`,
  where the date control reads `02/02/0421` and the "Available to borrow as
  of 0421-02-02" hint shows `0.0000 shares`).
- The form's JS (`create.blade.php:174-181`) on `change` of the date input
  ran `fetchAvailable()` against the bogus past date `0421-02-02`, got
  `available=0`, and `syncOverBorrow()` set `submitBtn.disabled = true`.
- `$browser->press('Open')` then tried to click a disabled button — Chrome
  reported `ElementClickInterceptedException` with the parent `<form>` as
  the recipient.

**Fix:** set the date via JS rather than typing into the date input:

```php
$browser->script(
    "var el = document.querySelector('input[name=\"origination_date\"]');"
    . "el.value = '" . $originationDate . "';"
    . "el.dispatchEvent(new Event('change', {bubbles:true}));"
);
```

This is the standard Selenium pattern for HTML5 `<input type="date">`.
`dispatchEvent` is required so the form's `change` listener still runs
`syncWarning()` + `fetchAvailable()` and the page re-renders the warning
and the (now correct) available-to-borrow figure.

**Result of fix:** S9 now drives past the form successfully, creates the
backdated CL, and reaches the show-page assertions. New failure is at
line 47 — `assertSeeIn('table.table', 'Late')` — tracked separately as
#ui-7.

This was **not a CSS / overlay bug**. The "form intercepts click" message
is what Chrome emits any time the click target is disabled; the parent
form just happens to be the next element in the stacking context.

### Original investigation (kept for provenance)

- **Smoking gun (exception):**
  > `ElementClickInterceptedException: Element <button type="submit"
  > class="btn btn-primary" id="submit-btn" disabled="">...</button> is not
  > clickable at point (61, 873).`

## P1 / #ui-7 — S9 schedule table missing "Late" label

**✅ Closed in test code.** Two issues compounded:

1. **Backdate too short.** A 30-day backdated origination produces a
   monthly schedule whose **first** payment is due ~1 month after
   origination — i.e. due today. Today is inside the 3-day default
   grace, so `DrawService::detectForLine` (which runs synchronously at
   create — confirmed at `DrawService.php:117`) doesn't flag any row
   yet. With 90 days of backdating, the first 2–3 rows are well past
   grace and get flagged correctly. Verified via tinker — the latest
   CL's schedule shows rows `2026-03-20 late / 2026-04-20 late /
   2026-05-20 late / ...` in `credit_line_payments`.
2. **Case mismatch.** The pill is rendered by
   `account_credit_lines/_payment_status_badge.blade.php:11` as
   `<span class="badge bg-danger">late</span>` — lowercase. The
   assertion `assertSeeIn('table.table', 'Late')` (capital L) didn't
   match.

Also widened the selector from `table.table` → `table` (the rendered
table on the show page doesn't carry the `.table` CSS class anyway).
Test now passes (4 assertions).

## P2 / #ui-bb1 — `tour` skill hardcodes credit-line filter

**✅ Closed.** Two fixes, both outside the repo (the helper script + the
test-env skill doc both live under `~/`):

1. `~/.familyfund-pool/testpool.sh::cmd_tour()` — default args changed
   from `--filter=CreditLineUITourTest` to `tests/Browser/`, so a no-arg
   invocation now runs the full suite. The comment above the function is
   updated to match.
2. `~/.claude/skills/test-env/SKILL.md` — the "Run Dusk UI tours" section
   now documents the new default and shows the `--filter=...` override
   pattern explicitly, so a reviewer reading the skill at face value gets
   the right behaviour without having to read the bash.

Verified by running `~/.familyfund-pool/testpool.sh tour test1` with no
filter args — Dusk reports `Tests: 13 failed, 14 passed (62 assertions)`,
i.e. the full 27-test suite ran (previously it would have run just one
test).

---

## Resolved-on-triage

- **Spatie seeders absent from baseline** — already documented as `#bb1` in
  [`QA_BUGS_2026-05-21.md`](QA_BUGS_2026-05-21.md#p2--bb1-testpool-baseline-is-stale-misses-one-migration).
  Not repeated here. Without running `RolesAndPermissionsSeeder` after a
  fresh claim, **every** Dusk test that needs an authenticated session
  would fail before even hitting the form.
- **`QaTestUsersSeeder` schema mismatch (`accounts.type`)** — already
  documented as
  [`#14`](QA_BUGS_2026-05-21.md#p2--14-qatestusersseeder-writes-non-existent-accountstype-column).
  Manifests during the Dusk pre-flight too; partial seed leaves the
  beneficiary's QA *account* missing but the *user* is created (so
  fund-admin / financial-manager / claude can still log in).

## Environment + cleanup

This run used `~/.familyfund-pool/testpool.sh` slot `test1`
(`familyfund_test1` DB on `db-testpool`). The slot is **still leased** at
the end of this run — release with:

```bash
~/.familyfund-pool/testpool.sh release test1
```

Screenshots from the run are committed to
`app1/family-fund-app/tests/Browser/screenshots/tour/` (only 01–04 of the
tour script — 05+ were never captured because of the timeout).
